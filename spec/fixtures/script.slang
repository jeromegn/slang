script src="https://somecdn/vue.min.js"

javascript:
  var num = 8*3;
  console.log(num);

script var num = 8*4;

javascript:
  new Vue({
    el: '#app',
    template: `
      <div>
        something
      </div>
    `
  })

javascript:
  let twoLines = "bar\nbaz";
  let hello = "#{"Hello, " + "world!"}";
  let obj = #{{:a => 17, "b" => "foo"}.to_json};
